import { AllowedValuesDirective } from './allowed-values.directive';

describe('AllowedValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedValuesDirective();
    expect(directive).toBeTruthy();
  });
});
